name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Runner is working!"

      - name: Show info
        run: |
          echo "OS: $(uname -a)"
          echo "Date: $(date)"
